name: 'My Action'
description: 'Description of my action'
inputs:
  github-token:
    description: 'GitHub token'
    required: false
runs:
  using: 'docker'
  image: 'docker://ghcr.io/gsmet/my-github-action:main'